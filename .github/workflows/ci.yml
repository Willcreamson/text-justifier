name: CI

on:
  push:
    branches:
      - main
      - master
      - dev
  pull_request:
    branches:
      - main
      - master
      - dev

jobs:
  build-and-test:
    name: Build and Test
    runs-on: ubuntu-latest

    strategy:
      matrix:
        node-version: [20.x] # Utilise la dernière version 20.x automatiquement

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4 # Version plus récente

      - name: Setup Node.js
        uses: actions/setup-node@v4 # Version plus récente
        with:
          node-version: ${{ matrix.node-version }}
          cache: 'npm' # Cache npm pour accélérer les builds

      - name: Install dependencies
        run: npm ci
      - name: Build TypeScript
        run: npx tsc --noEmit

      - name: Run tests
        run: npx vitest run --coverage
        env:
          NODE_ENV: test

      - name: Upload coverage
        uses: actions/upload-artifact@v4
        with:
          name: coverage-report
          path: coverage/   # ou ./reports/coverage si tu as changé le dossier